# Write your MySQL query statement below
# SQL schema
"""
Create table If Not Exists Person (id int, email varchar(255))
Truncate table Person
insert into Person (id, email) values ('1', 'a@b.com')
insert into Person (id, email) values ('2', 'c@d.com')
insert into Person (id, email) values ('3', 'a@b.com')
"""
# solution 1
# Runtime: 303 ms, faster than 87.73% of MySQL online submissions for Duplicate Emails.
SELECT DISTINCT
    T1.email AS Email
FROM
    Person AS T1,
    Person AS T2
WHERE
    T1.email = T2.email
    AND T1.id <> T2.id;


# solution2
# Runtime: 318 ms, faster than 79.62% of MySQL online submissions for Duplicate Emails.
SELECT
    email AS Email
FROM
    Person
GROUP BY email HAVING count(*) > 1

